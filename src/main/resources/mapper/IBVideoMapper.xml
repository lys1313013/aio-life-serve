<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.life.record.mapper.IBVideoMapper">

    <select id="getStatusCount" resultType="top.aiolife.record.pojo.vo.StatusCount">
        SELECT status, count(*) AS count
        FROM bilibili_video
        WHERE is_deleted = 0 and user_id = #{userId}
        GROUP BY status
    </select>


    <select id="getWatchTime" resultType="java.lang.Integer">
        SELECT sum(watched_duration) AS count
        FROM bilibili_video
        WHERE is_deleted = 0 and user_id = #{userId} AND status = 5
    </select>

    <select id="getTotalTime" resultType="java.lang.Integer">
        SELECT sum(duration) AS count
        FROM bilibili_video
        WHERE is_deleted = 0 and user_id = #{userId}
    </select>

</mapper>