<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lys.record.mapper.ITimeRecordEntity">

    <select id="recommendType" resultType="com.lys.record.pojo.entity.TimeRecordEntity">
        SELECT *
        FROM time_record
        WHERE is_deleted = 0 and user_id = #{userId} and date = #{date} and start_time &lt;= #{time} and end_time >= #{time}
    </select>

</mapper>